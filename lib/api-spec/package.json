{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs'),p='../../lib/api-zod/src/index.ts';let c=fs.existsSync(p)?fs.readFileSync(p,'utf8'):'';c=c.split('\\n').filter(l=>!l.includes('./generated/types')).join('\\n');fs.writeFileSync(p,c)\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.9.1"
  }
}
